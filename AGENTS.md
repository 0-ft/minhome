# Dev workflow
In general, development will be taking place while the dev container setup is running.
The host may not have pnpm installed - if you need to execute pnpm commands (e.g. adding dependencies) you should ALWAYS run them from within the dev container.